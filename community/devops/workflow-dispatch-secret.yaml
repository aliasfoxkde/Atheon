name: workflow-dispatch-secret
match: workflow_dispatch.*(?:password|token|secret|key)\s*:\s*\$\{\{\s*secrets\.
severity: high
enabled: true
description: 'Pattern: workflow-dispatch-secret — Detects secrets passed to workflow_dispatch inputs. Secrets should not be passed as workflow inputs as they may be exposed in logs.'