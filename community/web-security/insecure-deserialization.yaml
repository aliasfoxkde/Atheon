name: insecure-deserialization
match: pickle\.loads\(|yaml\.load\(|json\.loads\(|eval\(.*str\(|marshal\.loads\(
severity: critical
language: python
category: web-security
description: 'Pattern: ''insecure deserialization''. Web security vulnerability that
  could be exploited by attackers. This is a critical security issue that could lead
  to data breaches or system compromise.'
