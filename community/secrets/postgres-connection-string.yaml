name: postgres-connection-string
match: postgres://[A-Za-z0-9_:-]+@[A-Za-z0-9.-]+:[0-9]+/[A-Za-z0-9_]+
severity: high
description: 'Pattern: ''postgres connection string''. Pattern in category ''''. This
  is a high-severity issue that could lead to security vulnerabilities or significant
  quality problems.'
