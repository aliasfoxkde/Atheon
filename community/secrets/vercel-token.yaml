name: vercel-token
# Vercel tokens are 24 characters using the charset [A-Za-z0-9_].
# Anchored to assignment context to avoid firing on generic 24-char IDs.
match: '(?i)(?:VERCEL_TOKEN|vercel.?token)\s*[=:]\s*[A-Za-z0-9_]{24}'
severity: high
enabled: true
