name: datadog-api-key
# Datadog API keys are exactly 32 hex characters and always assigned to
# DD_API_KEY (or rarely dd_api_key) as an environment variable or config value.
match: '(?i)DD_API_KEY\s*[=:]\s*[0-9a-f]{32}'
severity: high
enabled: true
