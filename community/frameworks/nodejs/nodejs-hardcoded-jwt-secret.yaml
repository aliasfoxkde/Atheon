name: nodejs-hardcoded-jwt-secret
# Hardcoded JWT signing secret passed to jwt.sign() or jsonwebtoken.
match: '\b(jwt\.sign|jsonwebtoken\.sign)\s*\(\s*[^,]+,\s*["\x27][A-Za-z0-9_\-]{16,}["\x27]'
severity: high
enabled: true