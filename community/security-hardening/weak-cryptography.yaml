name: weak-cryptography
match: md5|sha1.*hash|crypto\.createCipher|Math\.random\(\).*password|Math\.random\(\).*token
severity: high
description: 'Pattern: ''weak cryptography''. Pattern in category ''''. This is a
  high-severity issue that could lead to security vulnerabilities or significant quality
  problems.'
