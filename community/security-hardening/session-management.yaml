name: session-management
match: session.*secret.*random|session.*cookie.*secure.*false|session.*maxAge.*0
severity: high
description: 'Pattern: ''session management''. Pattern in category ''''. This is a
  high-severity issue that could lead to security vulnerabilities or significant quality
  problems.'
