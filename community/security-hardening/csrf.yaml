name: csrf
match: form.*action.*POST|<form.*method.*post.*csrf.*false|cookie.*sameSite.*none
severity: high
description: 'Detected missing CSRF protection: ''csrf''. Forms should include CSRF
  tokens to prevent cross-site request forgery. This is a high-severity issue that
  could lead to security vulnerabilities or significant quality problems.'
