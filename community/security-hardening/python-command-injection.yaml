name: python-command-injection
match: os\.system\(.*\}|subprocess\.call\(.*\}|eval\(.*request\.|exec\(.*request\.
severity: critical
language: python
category: security-hardening
description: 'Detected potential SQL injection risk: ''python command injection''.
  User input should be properly sanitized before use in database queries. This is
  a critical security issue that could lead to data breaches or system compromise.'
